# AI News Assistant Bot

Telegram-бот для автоматического сбора, фильтрации и перевода новостей об искусственном интеллекте.

## Что делает бот?

1. **Собирает новости** из 6 топовых источников об AI (TechCrunch, VentureBeat, MIT Tech Review и др.)
2. **Умный анализ через Claude AI:**
   - **70% - Практика**: новые инструменты, инвестиции, бизнес-кейсы
   - **30% - Контекст**: регулирование, исследования, макротренды
   - Итого: **полная картина AI-индустрии** с фокусом на применимость
3. **Переводит на русский** и создает краткие выжимки (8-12 новостей)
4. **Отправляет в Telegram** каждое утро в 08:00 (настраивается)

## Быстрый старт

### Шаг 1: Установка зависимостей

Убедитесь, что у вас установлен Python 3.8+

```bash
cd D:\AI\news-assistant-bot
pip install -r requirements.txt
```

### Шаг 2: Получение API ключей

#### 2.1 Claude API Key

1. Зайдите на https://console.anthropic.com/
2. Зарегистрируйтесь или войдите
3. Перейдите в "API Keys"
4. Создайте новый ключ
5. Скопируйте его (понадобится для .env файла)

**Стоимость:** ~$1-5 в месяц для вашей задачи (оплата по факту использования)

#### 2.2 Telegram Bot Token

1. Откройте Telegram
2. Найдите бота @BotFather
3. Отправьте команду `/newbot`
4. Придумайте имя бота (например, "My AI News Bot")
5. Придумайте username (должен заканчиваться на "bot", например "my_ai_news_bot")
6. Скопируйте токен, который вам выдаст BotFather

#### 2.3 Ваш Telegram User ID

1. Найдите бота @userinfobot в Telegram
2. Нажмите Start
3. Он отправит вам ваш User ID (число типа 123456789)
4. Скопируйте это число

### Шаг 3: Настройка

Создайте файл `.env` в корне проекта (скопируйте из `.env.example`):

```bash
copy .env.example .env
```

Откройте `.env` блокнотом и заполните:

```env
ANTHROPIC_API_KEY=sk-ant-ваш-ключ-от-claude
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_USER_ID=123456789
DIGEST_TIMES=09:00,18:00
```

### Шаг 4: Запуск

#### Разовый запуск (для теста)

```bash
python main.py
```

Бот соберет новости за последние 24 часа и отправит вам дайджест.

#### Постоянная работа по расписанию

```bash
python scheduler.py
```

Бот будет работать постоянно и отправлять дайджесты в указанное время (09:00 и 18:00 по умолчанию).

**Важно:** Компьютер должен быть включен во время отправки!

## Структура проекта

```
news-assistant-bot/
├── main.py              # Основной скрипт (разовый запуск)
├── scheduler.py         # Запуск по расписанию
├── requirements.txt     # Зависимости Python
├── .env                 # Ваши API ключи (создать самому)
├── .env.example         # Пример конфигурации
├── README.md           # Эта инструкция
│
├── config/
│   └── rss_feeds.json  # Список RSS-источников новостей
│
├── src/
│   ├── rss_parser.py   # Парсинг RSS-фидов
│   ├── ai_processor.py # Обработка через Claude
│   ├── telegram_bot.py # Отправка в Telegram
│   └── database.py     # База данных (SQLite)
│
├── data/               # База данных (создается автоматически)
└── logs/               # Логи (для будущего)
```

## Настройка источников новостей

Откройте `config/rss_feeds.json` для добавления/удаления источников:

```json
[
  {
    "name": "TechCrunch AI",
    "url": "https://techcrunch.com/category/artificial-intelligence/feed/",
    "enabled": true
  }
]
```

Можно:
- Отключить источник: `"enabled": false`
- Добавить новый RSS-фид

## Настройка расписания

В файле `.env` измените `DIGEST_TIMES`:

```env
# Один раз в день (утром)
DIGEST_TIMES=09:00

# Три раза в день
DIGEST_TIMES=08:00,14:00,20:00
```

## Решение проблем

### Ошибка: "ANTHROPIC_API_KEY not found"

Вы не создали файл `.env` или не заполнили ключ Claude API.

### Ошибка: "Telegram error"

Проверьте:
1. Правильно ли скопирован TELEGRAM_BOT_TOKEN
2. Правильно ли указан TELEGRAM_USER_ID
3. Вы нажали Start у вашего бота в Telegram

### Бот не отправляет дайджесты

1. Проверьте, что компьютер включен в указанное время
2. Проверьте правильность времени в `DIGEST_TIMES`
3. Посмотрите консоль - там будут ошибки

### "No articles found"

RSS-фиды могут быть временно недоступны. Подождите и попробуйте снова.

## Стоимость

- **Claude API**: ~$1-5/месяц (оплата по факту, первые $5 часто бесплатно)
- **Telegram**: Бесплатно
- **Хостинг**: $0 (работает на вашем компьютере) или $5/месяц (VPS)

## Следующие шаги

1. **Тест**: Запустите `python main.py` для проверки
2. **Автозапуск**: Настройте `scheduler.py` для автоматической работы
3. **VPS (опционально)**: Если нужна работа 24/7, перенесите на сервер

## Поддержка

Если что-то не работает - проверьте:
1. Установлены ли все зависимости (`pip install -r requirements.txt`)
2. Создан ли файл `.env` с правильными ключами
3. Есть ли интернет-соединение
4. Нажали ли Start у бота в Telegram

---

**Готово!** Ваш AI News Assistant готов к работе 🚀

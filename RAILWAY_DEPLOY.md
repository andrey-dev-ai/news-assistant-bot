# 🚂 Деплой AI News Bot на Railway

Простая инструкция по развертыванию бота на Railway для работы 24/7.

---

## 📋 Что понадобится

1. Аккаунт на Railway.app (бесплатный tier - 500 часов/месяц)
2. GitHub аккаунт (для подключения репозитория)
3. Ваши API ключи (уже есть в .env)

---

## 🚀 Способ 1: Деплой через GitHub (Рекомендуется)

### Шаг 1: Создайте GitHub репозиторий

1. Зайдите на https://github.com/new
2. Создайте новый **приватный** репозиторий (назовите `ai-news-bot`)
3. **НЕ** добавляйте README, .gitignore или LICENSE

### Шаг 2: Загрузите код на GitHub

Откройте PowerShell в папке проекта:

```powershell
cd D:\AI\news-assistant-bot

# Инициализируйте git (если ещё не сделано)
git init

# Добавьте все файлы (кроме .env - он в .gitignore)
git add .

# Создайте первый коммит
git commit -m "Initial commit: AI News Assistant Bot"

# Подключите удалённый репозиторий
git remote add origin https://github.com/ВАШ_USERNAME/ai-news-bot.git

# Отправьте код
git branch -M main
git push -u origin main
```

### Шаг 3: Деплой на Railway

1. Зайдите на https://railway.app/
2. Нажмите **"Login"** → войдите через GitHub
3. Нажмите **"New Project"**
4. Выберите **"Deploy from GitHub repo"**
5. Выберите репозиторий `ai-news-bot`
6. Railway автоматически определит Dockerfile и начнёт деплой

### Шаг 4: Настройте переменные окружения

1. В Railway проекте откройте ваш сервис
2. Перейдите на вкладку **"Variables"**
3. Добавьте переменные (нажимайте **"+ New Variable"**):

```
ANTHROPIC_API_KEY=your-anthropic-api-key-here
TELEGRAM_BOT_TOKEN=8423032550:AAHqwMmqi-dVF9g8YmEk5HYGjWKP5J8A0oU
TELEGRAM_USER_ID=5260209994
DIGEST_TIMES=08:00
```

4. Railway автоматически передеплоит с новыми переменными

### Шаг 5: Проверка

1. Перейдите на вкладку **"Deployments"**
2. Дождитесь статуса **"Success"** (зелёный)
3. Откройте **"Logs"** чтобы увидеть работу бота
4. Дождитесь времени отправки (08:00) или проверьте логи

---

## 🚀 Способ 2: Деплой через Railway CLI

### Установка Railway CLI

```powershell
# Установите Railway CLI
npm install -g @railway/cli

# Или через Scoop (если установлен)
scoop install railway
```

### Деплой

```powershell
cd D:\AI\news-assistant-bot

# Авторизуйтесь
railway login

# Создайте новый проект
railway init

# Добавьте переменные окружения
railway variables set ANTHROPIC_API_KEY="your-anthropic-api-key-here"
railway variables set TELEGRAM_BOT_TOKEN="8423032550:AAHqwMmqi-dVF9g8YmEk5HYGjWKP5J8A0oU"
railway variables set TELEGRAM_USER_ID="5260209994"
railway variables set DIGEST_TIMES="08:00"

# Задеплойте
railway up
```

---

## 📊 Управление ботом на Railway

### Просмотр логов в реальном времени

В Railway dashboard:
1. Откройте ваш сервис
2. Вкладка **"Logs"**
3. Или через CLI: `railway logs`

### Перезапуск бота

1. В Railway dashboard: **Settings** → **Restart**
2. Или через CLI: `railway restart`

### Обновление кода

После изменений в коде:

```powershell
cd D:\AI\news-assistant-bot

git add .
git commit -m "Update bot code"
git push

# Railway автоматически передеплоит!
```

### Изменить расписание

1. Railway dashboard → **Variables**
2. Измените `DIGEST_TIMES` (например: `08:00,18:00` для двух раз в день)
3. Бот автоматически перезапустится

---

## 💰 Стоимость

### Railway Free Tier
- 500 часов выполнения/месяц
- $5 кредитов/месяц
- Этого **достаточно** для бота, работающего 24/7!

### Расчёт
- Бот: 720 часов/месяц (24/7)
- НО: бот использует ресурсы только при отправке
- Фактически: ~10-20 часов активности
- **Итого: полностью в рамках free tier**

### Если нужно больше
- Hobby Plan: $5/месяц (вместо VPS за €4.15)
- Без лимитов по часам

---

## 🔧 Настройка временной зоны

Railway работает в UTC. Для Киева (UTC+2):

Два варианта:

### Вариант 1: Указать киевское время
В переменных окружения `DIGEST_TIMES=08:00` - это будет UTC+2/+3 автоматически

### Вариант 2: Указать UTC время
Если хотите в 08:00 по Киеву:
- Зимой (UTC+2): `DIGEST_TIMES=06:00`
- Летом (UTC+3): `DIGEST_TIMES=05:00`

---

## 🛠️ Troubleshooting

### Бот не отправляет сообщения

Проверьте:
1. **Logs** - есть ли ошибки?
2. **Variables** - все ключи правильные?
3. Вы нажали **Start** у бота в Telegram?

### Деплой завершился с ошибкой

1. Проверьте логи деплоя
2. Убедитесь, что все файлы (Dockerfile, requirements.txt) загружены в GitHub
3. Попробуйте **Redeploy**

### База данных сбрасывается

Railway использует эфемерное хранилище. Для SQLite это нормально - база пересоздаётся при каждом деплое, но это не критично для бота (он только отслеживает отправленные новости).

Если нужна постоянная БД:
1. Railway dashboard → **"+ New"** → **PostgreSQL**
2. Измените код для использования PostgreSQL вместо SQLite

---

## ✅ Готово!

После успешного деплоя:
- ✅ Бот работает 24/7
- ✅ Отправляет дайджесты по расписанию
- ✅ Автоматически перезапускается при сбоях
- ✅ Логи доступны в любое время
- ✅ Бесплатно (в рамках free tier)

**Следующий дайджест придёт в 08:00 по киевскому времени!** 🎉

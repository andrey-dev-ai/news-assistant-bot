# Ребрендинг news-assistant-bot

## Статус: ✅ ЗАВЕРШЁН (30.01.2026)

### Что изменилось

| Было | Стало |
|------|-------|
| @ai_dlya_mamy | @ai_dlya_doma |
| Женщины 25-45 | Все, кто интересуется AI |
| Короткие посты (350 символов) | Длинные посты (1000-1500 символов) |
| Ссылка в конце "👉 Попробовать" | Ссылки внутри текста |
| `parse_mode="Markdown"` | `parse_mode="HTML"` |

---

## Выполненные изменения

### 1. Классификатор (`src/post_generator.py:183-235`)
- Новая аудитория: все кто интересуется AI
- Новые критерии: инструменты, новости компаний, тренды, обновления моделей
- Убраны: "только для женщин", "только быт"

### 2. Генерация постов (`src/post_generator.py:324-381`)
- Новый универсальный промпт `_get_universal_prompt()`
- Длинный формат: 1000-1500 символов
- Ссылки внутри текста, без шаблонных CTA
- Удалены старые промпты

### 3. Логика картинок (`src/post_generator.py:116-203`)
- `is_good_image()` — проверка качества OG-картинок
- `generate_image_via_openai()` — генерация через DALL-E 3
- `get_image_for_post()` — гибридная логика (OG → DALL-E)

### 4. HTML парсинг (КРИТИЧЕСКИЙ ФИКС)
- `src/telegram_bot.py` — добавлен `parse_mode="HTML"` в preview и publish
- `scheduler.py` — добавлен `parse_mode="HTML"` в автопубликацию

---

## Коммиты

| Коммит | Описание |
|--------|----------|
| `cc9fbd3` | Новый формат постов и гибридные картинки |
| (текущий) | Фикс HTML парсинга в Telegram |

---

## Ключевые файлы

| Файл | Изменения |
|------|-----------|
| `src/post_generator.py` | Классификатор, генерация, картинки |
| `src/telegram_bot.py` | HTML парсинг в preview/publish |
| `scheduler.py` | HTML парсинг в автопубликации |
| `.env` | @ai_dlya_doma ✅ |

---

## Тестирование

```bash
# 1. Сгенерировать посты
/generate

# 2. Посмотреть превью (теги должны рендериться)
/preview

# 3. Опубликовать тестовый пост
/publish_now
```

## Что проверить в постах
- ✅ Длина 1000-1500 символов
- ✅ **Жирный заголовок** рендерится
- ✅ Ссылки кликабельные, внутри текста
- ✅ Нет "👉 Попробовать"

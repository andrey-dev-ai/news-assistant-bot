# Инструкция для продолжения ребрендинга

## Статус на 30.01.2026

### ✅ Сделано

1. **Классификатор обновлён** (`src/post_generator.py:183-235`)
   - Новая аудитория: все кто интересуется AI
   - Новые критерии: инструменты, новости компаний, тренды, обновления моделей
   - Убраны: только для женщин, только быт

2. **Закоммичено и запушено**
   - Коммит: `63b4ad0`
   - Сообщение: "WIP: Ребрендинг - обновлён классификатор для @ai_dlya_doma"

3. **OpenAI ключ добавлен** в `d:\AI\.secrets\credentials.md`

### ❌ Осталось сделать

#### 1. Переделать генерацию постов (ПРИОРИТЕТ)

**Файл:** `src/post_generator.py`

**Что изменить:**
- Методы `_get_ai_tool_prompt()`, `_get_quick_tip_prompt()`, `_get_prompt_day_prompt()` → заменить на один `_get_universal_prompt()`
- Убрать лимит 350 символов → новый лимит 1500-2000
- Новый стиль постов (как на Датаборде):
  ```
  [Эмодзи] Заголовок

  Первый абзац — суть.

  Второй абзац — детали. Ссылки <a href="url">внутри текста</a>.

  Третий абзац — контекст.
  ```
- Убрать шаблонные CTA типа "👉 Попробовать"

#### 2. Добавить логику картинок

**Новые функции в `src/post_generator.py`:**

```python
def is_good_image(url: str) -> bool:
    """Проверяет качество OG-картинки."""
    bad_patterns = ['placeholder', 'default', 'logo', 'icon', 'avatar', '1x1']
    if any(p in url.lower() for p in bad_patterns):
        return False
    if not any(url.lower().endswith(ext) for ext in ['.jpg', '.jpeg', '.png', '.webp']):
        return False
    return True
```

#### 3. Интегрировать GPT Image

**Добавить в `src/post_generator.py`:**

```python
from openai import OpenAI

def generate_image(self, prompt: str) -> Optional[str]:
    """Генерирует картинку через OpenAI."""
    try:
        client = OpenAI(api_key=os.getenv('OPENAI_API_KEY'))
        response = client.images.generate(
            model="dall-e-3",
            prompt=prompt,
            size="1024x1024",
            quality="standard",
            n=1
        )
        return response.data[0].url
    except Exception as e:
        logger.error(f"Image generation failed: {e}")
        return None
```

#### 4. Обновить конфиги

- `config/prompts.yaml` — убрать "женщины 25-45", обновить аудиторию
- `README.md` — переписать под @ai_dlya_doma

#### 5. Деплой

```bash
# Загрузить на VPS
scp "D:\AI\projects\news-assistant-bot\src\post_generator.py" root@141.227.152.143:/opt/news-assistant-bot/src/

# Перезапустить
ssh root@141.227.152.143 "systemctl restart ai-news-bot"

# Проверить логи
ssh root@141.227.152.143 "journalctl -u ai-news-bot -f"
```

#### 6. Контент-план и RSS (отложено)

Агенты были прерваны. Нужно запустить заново:
- Контент-план: рубрики, тональность, примеры
- RSS-источники: найти 5-10 новых источников

---

## Промпт для продолжения

```
Продолжи ребрендинг news-assistant-bot.

Прочитай инструкцию: d:\AI\projects\news-assistant-bot\docs\CONTINUE-REBRAND.md

Статус:
- Классификатор ✅ готов
- Генерация постов ❌ нужно переделать
- Картинки ❌ нужно добавить
- Деплой ❌

Начни с генерации постов — переделай промпты на длинный формат (1500-2000 символов), ссылки внутри текста, стиль как на Датаборде.

План в файле: C:\Users\aampo\.claude\plans\calm-noodling-wirth.md
```

---

## Ключевые файлы

| Файл | Что там |
|------|---------|
| `src/post_generator.py` | Классификация и генерация постов |
| `config/prompts.yaml` | Промпты (не используются в коде, но нужно обновить) |
| `.env` | Уже @ai_dlya_doma ✅ |
| `C:\Users\aampo\.claude\plans\calm-noodling-wirth.md` | Полный план работ |

## Скриншоты Датаборда

Пользователь показывал скриншоты канала "Датаборд" как образец дизайна постов:
- Длинные информативные посты
- Ссылки спрятаны в текст: `[прокоментувала](url)`
- Одна качественная картинка
- Энергичная подача

# План: Обновление news-assistant-bot по контент-стратегии

## Резюме изменений

**Было (Phase 2):**
- 5 постов/день = 35 постов/неделю, автопубликация
- 8 RSS источников
- Один универсальный формат поста
- Базовая классификация (relevant/not relevant)

**Станет (Phase 3):**
- 11 постов/неделю по рубрикам
- **Ручное одобрение каждого поста перед публикацией**
- 15 RSS источников с приоритетами
- 6 шаблонов постов по рубрикам
- Фильтрация consumer vs enterprise AI
- Система аналитики (ERR, охват)
- Опросы и fun-контент — ручное создание (бот даёт инструкции)

---

## Ключевое изменение: Система модерации

Вместо автоматической публикации — **workflow с одобрением**:

```
1. Бот генерирует пост → статус "pending_approval"
2. Бот присылает пост владельцу с inline-кнопками под постом
3. Владелец проверяет и нажимает кнопку
4. После одобрения → публикация в канал
```

### Постоянная клавиатура (ReplyKeyboardMarkup)

Всегда видна внизу экрана чата с ботом:

```
┌─────────────┬─────────────┐
│ 📋 Очередь  │ 📊 Статистика│
├─────────────┼─────────────┤
│ 🔄 Обновить │ ⚙️ Настройки │
└─────────────┴─────────────┘
```

**Кнопки:**
- **📋 Очередь** — показать посты ожидающие одобрения (список)
- **📊 Статистика** — статистика канала и постов
- **🔄 Обновить** — сгенерировать новые посты сейчас
- **⚙️ Настройки** — конфигурация бота

### Inline-кнопки (под каждым постом)

Когда бот присылает пост на модерацию:

```
[Превью поста здесь...]

┌─────────────────┬────────────────┐
│ ✅ Опубликовать │ 📅 Отложить    │
├─────────────────┼────────────────┤
│ ✏️ Редактировать│ ❌ Отклонить   │
└─────────────────┴────────────────┘
```

**Действия:**
- **✅ Опубликовать** — немедленная публикация в канал
- **📅 Отложить** — запланировать по расписанию
- **✏️ Редактировать** — открыть диалог редактирования
- **❌ Отклонить** — удалить из очереди

---

## Критические файлы

| Файл | Действие | Описание |
|------|----------|----------|
| `config/rss_feeds.json` | Изменить | Добавить 7 новых RSS источников |
| `config/content_plan.yaml` | Создать | Недельное расписание по рубрикам |
| `config/prompts.yaml` | Изменить | Шаблоны промптов для 6 рубрик |
| `src/rubrics.py` | Создать | Система рубрик и шаблонов |
| `src/content_filter.py` | Создать | Фильтрация consumer/enterprise |
| `src/schedule_manager.py` | Создать | Управление недельным расписанием |
| `src/analytics.py` | Создать | Сбор и анализ статистики |
| `src/moderation.py` | Создать | **Система одобрения постов** |
| `src/post_generator.py` | Изменить | Генерация по рубрикам |
| `src/telegram_bot.py` | Изменить | Постоянная клавиатура + inline-кнопки |
| `src/database.py` | Изменить | Таблицы post_stats, статусы постов |
| `scheduler.py` | Изменить | Новое недельное расписание |

---

## Этапы имплементации

### Этап 1: Конфигурация
1. Создать `config/content_plan.yaml` — недельное расписание
2. Обновить `config/rss_feeds.json` — добавить 7 источников
3. Добавить feature flags в `src/config.py`

### Этап 2: База данных
1. Создать миграцию `db/migrations/001_add_analytics.sql`
2. Добавить таблицы: post_stats, daily_metrics
3. Расширить post_queue (rubric, hashtags, day_of_week)

### Этап 3: Рубрики и фильтрация
1. Создать `src/rubrics.py` — 6 рубрик с конфигами
2. Создать `src/content_filter.py` — consumer vs enterprise
3. Обновить `config/prompts.yaml` — шаблоны для каждой рубрики

### Этап 4: Генератор постов
1. Рефакторинг `src/post_generator.py`:
   - Метод `generate_post_for_rubric(article, rubric)`
   - Расширенная классификация с определением рубрики
   - Интеграция с ContentFilter

### Этап 5: Система модерации (КЛЮЧЕВОЙ)
1. Создать `src/moderation.py`:
   - Класс `ModerationQueue` — управление очередью одобрения
   - Методы: `send_for_approval()`, `approve()`, `reject()`, `edit()`
2. Обновить `src/telegram_bot.py`:
   - **Постоянная клавиатура** (📋 Очередь, 📊 Статистика, 🔄 Обновить, ⚙️ Настройки)
   - **Inline-кнопки** под постами (✅ Опубликовать, 📅 Отложить, ✏️ Редактировать, ❌ Отклонить)
   - Callback handlers для всех кнопок
3. Обновить `src/database.py`:
   - Статусы: draft → pending_approval → approved → published
   - Поле `approved_at`, `approved_by`

### Этап 6: Планировщик
1. Создать `src/schedule_manager.py`
2. Обновить `scheduler.py`:
   - Генерация на неделю вперёд (воскресенье 07:00)
   - Отправка постов на модерацию вместо автопубликации
   - Напоминания о постах ожидающих одобрения

### Этап 7: Аналитика
1. Создать `src/analytics.py`
2. Интегрировать сбор статистики после публикации
3. Добавить команду `/analytics` в бота

### Этап 8: Тестирование и деплой
1. Unit-тесты для новых модулей
2. Локальное тестирование с feature flags
3. Деплой на VPS с постепенным включением

---

## Новое расписание публикаций

| День | Время | Рубрика | Хэштег |
|------|-------|---------|--------|
| Пн | 10:00 | AI-инструмент недели | #инструмент_недели |
| Пн | 19:00 | Опрос/голосование | — |
| Вт | 12:00 | Новости нейросетей | #новости |
| Вт | 20:00 | Промпт для дома | #промпт_дня |
| Ср | 11:00 | Лайфхак с AI | #лайфхак |
| Ср | 21:00 | До/После генерации | — |
| Чт | 10:00 | Обновление сервиса | #новости |
| Чт | 19:00 | Бесплатный сервис | #бесплатно |
| Пт | 12:00 | Подборка недели | #подборка |
| Сб | 15:00 | Fun-контент/мем | — |
| Вс | 21:00 | Дайджест недели | #дайджест |

---

## Новые RSS источники (добавить к существующим 8)

```json
[
  {"name": "Wired AI", "url": "https://www.wired.com/feed/tag/ai/latest/rss", "priority": 2},
  {"name": "The Guardian AI", "url": "https://www.theguardian.com/technology/artificialintelligenceai/rss", "priority": 3},
  {"name": "Ars Technica AI", "url": "https://feeds.arstechnica.com/arstechnica/ai", "priority": 3},
  {"name": "Ben's Bites", "url": "https://rss.beehiiv.com/feeds/2R3C6Bt5wj.xml", "priority": 2},
  {"name": "Habr ML", "url": "https://habr.com/ru/rss/hub/machine_learning/all/?fl=ru", "priority": 2},
  {"name": "VC.ru", "url": "https://vc.ru/rss/all", "priority": 3},
  {"name": "Product Hunt", "url": "https://www.producthunt.com/feed", "priority": 3}
]
```

---

## Feature Flags (постепенное включение)

```bash
# .env
USE_NEW_SCHEDULE=false    # Новое недельное расписание
USE_RUBRICS=false         # Генерация по рубрикам
COLLECT_ANALYTICS=false   # Сбор статистики
```

---

## Верификация

1. **Локальное тестирование:**
   ```bash
   pytest tests/ -v
   python -c "from src.rubrics import RubricManager; rm = RubricManager(); print(rm.get_slots_for_week())"
   ```

2. **Проверка RSS:**
   ```bash
   python -c "from src.rss_parser import RSSParser; print(len(RSSParser().fetch_recent_news()))"
   ```

3. **Тест генерации по рубрике:**
   ```bash
   python -m src.post_generator --rubric tool_review --dry-run
   ```

4. **Деплой:**
   ```bash
   scp -r src/ config/ root@141.227.152.143:/opt/news-assistant-bot/
   ssh root@141.227.152.143 "systemctl restart ai-news-bot"
   ```

---

## Ручной контент: Опросы и Fun

### Опросы (Пн 19:00) — ручное создание

Бот будет напоминать о создании опроса и давать идеи:
```
📊 Время опроса! Идеи на эту неделю:

1. "Какой AI-сервис используете чаще всего?"
   - ChatGPT / Claude / Gemini / Другой

2. "Для чего используете AI дома?"
   - Работа / Учёба / Творчество / Быт

3. По теме недельного дайджеста
```

**Создание опроса в Telegram:**
1. Открыть канал @ai_dlya_doma
2. Нажать 📎 → Опрос
3. Ввести вопрос и варианты

### Fun-контент/мемы (Сб 15:00) — ручное создание

**Источники идей для мемов:**
- r/ProgrammerHumor (AI-тематика)
- Twitter/X: @aihumor, @techhumor
- imgflip.com/meme-generator (шаблоны)
- Генерация через DALL-E: "meme about AI doing [task]"

**Инструкция:**
1. Найти шаблон или идею
2. Адаптировать под русскоязычную аудиторию
3. Добавить текст через Canva/Figma
4. Опубликовать с коротким комментарием

Бот будет напоминать в субботу утром:
```
😄 Суббота = день мема!

Идеи на сегодня:
- "Когда ChatGPT не понимает твой запрос"
- "AI vs дедлайн"
- "Ожидание vs реальность генерации"
```

---

## Решения (подтверждено)

- ✅ Частота: 11 постов/неделю
- ✅ Модерация: ручное одобрение каждого поста
- ✅ Опросы: ручное создание с подсказками от бота
- ✅ Fun-контент: ручное создание с источниками идей
- ✅ UI: постоянная клавиатура + inline-кнопки под постами

---

## Статус имплементации (2026-01-31)

### ✅ Выполнено

| Компонент | Статус | Коммит |
|-----------|--------|--------|
| Feature flags в config.py | ✅ Done | `05226b4` |
| Система модерации (moderation.py) | ✅ Done | `05226b4` |
| Постоянная клавиатура | ✅ Done | `05226b4` |
| Inline-кнопки под постами | ✅ Done | `05226b4` |
| Рубрики (rubrics.py) | ✅ Done | `05226b4` |
| 15 RSS источников | ✅ Done | `05226b4` |
| content_plan.yaml | ✅ Done | `05226b4` |
| Исправление лимита caption (700-900 символов) | ✅ Done | `e509afe` |

### 🔄 В разработке

| Компонент | Статус | Примечание |
|-----------|--------|------------|
| USE_RUBRICS | Готово, не включено | Включить после тестирования |
| USE_NEW_SCHEDULE | Готово, не включено | Включить после отладки |
| Аналитика (analytics.py) | Не начато | Этап 7 |
| Content filter | Не начато | consumer vs enterprise |

### Текущая конфигурация (.env на сервере)

```bash
USE_MODERATION=true
USE_RUBRICS=false
USE_NEW_SCHEDULE=false
```

### Известные ограничения

- **Telegram caption**: максимум 1024 символа → посты генерируются 700-900 символов
- Рубрики и новое расписание требуют дополнительного тестирования

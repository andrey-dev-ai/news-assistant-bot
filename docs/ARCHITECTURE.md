# Архитектура проекта AI News Bot

> Последнее обновление: 2026-01-22
> Версия: 2.0 (Phase 2)

---

## Обзор

Telegram-бот для канала **@ai_dlya_doma** — автоматическая публикация AI-новостей для аудитории "женщины 25-45, быт и семья".

### Два режима работы

| Режим | Описание | Команда |
|-------|----------|---------|
| **Legacy (Phase 1)** | 1 длинный дайджест в день | `/post`, `/digest` |
| **Phase 2 (новый)** | 5 отдельных постов с картинками | `/generate`, `/publish_now` |

---

## Структура файлов

```
news-assistant-bot/
├── main.py                 # Разовый запуск (тест)
├── scheduler.py            # Основной запуск (5 постов/день)
│
├── src/
│   ├── config.py           # Pydantic Settings (валидация .env)
│   ├── logger.py           # Logging с ротацией
│   ├── rss_parser.py       # RSS парсинг + tenacity retry
│   ├── ai_processor.py     # Claude API (дайджест) + retry
│   ├── telegram_bot.py     # TelegramSender + TelegramBotHandler
│   ├── database.py         # SQLite: sent_articles
│   ├── post_generator.py   # [NEW] Генерация отдельных постов
│   ├── post_queue.py       # [NEW] SQLite очередь постов
│   └── image_generator.py  # [TODO] DALL-E интеграция
│
├── config/
│   └── rss_feeds.json      # Список RSS источников
│
├── data/
│   └── news_bot.db         # SQLite база данных
│
├── logs/
│   ├── news_bot.log        # Основной лог (5MB, 5 backups)
│   └── news_bot_errors.log # Только ошибки
│
├── docs/                   # Документация
│   ├── ARCHITECTURE.md     # ← Этот файл
│   ├── PHASE2-ROADMAP.md   # План Phase 2
│   ├── technical_research.md
│   ├── content_strategy.md
│   └── monetization.md
│
├── .env                    # Конфигурация (НЕ в git!)
├── .env.example            # Пример конфигурации
└── requirements.txt        # Python зависимости
```

---

## Компоненты

### 1. config.py — Конфигурация

```python
from config import get_settings, validate_config

settings = get_settings()
print(settings.telegram_channel_id)  # @ai_dlya_doma
```

**Переменные окружения:**

| Переменная | Обязательная | Описание |
|------------|--------------|----------|
| `ANTHROPIC_API_KEY` | Да | Claude API ключ |
| `TELEGRAM_BOT_TOKEN` | Да | Токен от @BotFather |
| `TELEGRAM_USER_ID` | Да | ID владельца бота |
| `TELEGRAM_CHANNEL_ID` | Нет | ID канала (@username или число) |
| `DIGEST_TIMES` | Нет | Времена дайджеста (default: "08:00") |
| `PUBLISH_TIMES` | Нет | Времена постов (default: "09:00,12:00,15:00,18:00,21:00") |
| `LOG_LEVEL` | Нет | DEBUG/INFO/WARNING/ERROR |
| `LOG_DIR` | Нет | Папка логов (default: "logs") |

---

### 2. post_generator.py — Генерация постов

**Классы:**
- `PostFormat` — Enum форматов (AI_TOOL, QUICK_TIP, PROMPT_DAY)
- `GeneratedPost` — Dataclass готового поста
- `PostGenerator` — Основной класс

**Ключевые методы:**

```python
generator = PostGenerator()

# Классификация статьи (Haiku, дешёво)
result = generator.classify_article(article)
# → {"relevant": True, "confidence": 85, "category": "tool", "format": "ai_tool"}

# Генерация поста (Sonnet, качество)
post = generator.generate_post(article, PostFormat.AI_TOOL)
# → GeneratedPost(text="🤖 AI-находка...", image_prompt="flat design...")

# Полный пайплайн
posts = generator.generate_daily_posts(articles, count=5)
```

**Модели Claude:**
- `claude-3-5-haiku-20241022` — классификация, image prompts (дёшево)
- `claude-sonnet-4-20250514` — генерация постов (качество)

---

### 3. post_queue.py — Очередь постов

**Схема БД:**

```sql
CREATE TABLE post_queue (
    id INTEGER PRIMARY KEY,
    article_url TEXT,
    article_title TEXT,
    post_text TEXT NOT NULL,
    image_url TEXT,           -- URL картинки (после генерации)
    image_prompt TEXT,        -- Промпт для DALL-E
    format TEXT,              -- 'ai_tool', 'quick_tip', 'prompt_day'
    scheduled_at DATETIME,    -- Когда публиковать
    published_at DATETIME,    -- Когда опубликовано
    status TEXT,              -- 'pending', 'published', 'failed'
    created_at DATETIME,
    error_message TEXT
);
```

**Методы:**

```python
queue = PostQueue()

# Добавить пост
post_id = queue.add_post(text="...", scheduled_at=datetime(...))

# Запланировать 5 постов на день
queue.schedule_posts_for_day(posts, times=["09:00", "12:00", ...])

# Получить следующий для публикации
post = queue.get_next_pending()

# Отметить как опубликованный
queue.mark_published(post_id)
```

---

### 4. telegram_bot.py — Telegram интеграция

**Классы:**
- `TelegramSender` — HTTP API (send_message, send_to_channel)
- `TelegramBotHandler` — Команды бота

**Команды бота:**

| Команда | Описание | Авторизация |
|---------|----------|-------------|
| `/start` | Приветствие | Все |
| `/help` | Справка | Все |
| `/digest` | Получить дайджест (legacy) | Все |
| `/post` | Опубликовать дайджест в канал | Только owner |
| `/generate` | Сгенерировать 5 постов | Только owner |
| `/preview` | Посмотреть запланированные посты | Все |
| `/publish_now` | Опубликовать следующий пост | Только owner |

---

### 5. scheduler.py — Планировщик

**Расписание:**

| Время | Действие |
|-------|----------|
| 08:00 | `generate_daily_posts()` — генерация 5 постов |
| 09:00 | `publish_scheduled_post()` — публикация #1 |
| 12:00 | `publish_scheduled_post()` — публикация #2 |
| 15:00 | `publish_scheduled_post()` — публикация #3 |
| 18:00 | `publish_scheduled_post()` — публикация #4 |
| 21:00 | `publish_scheduled_post()` — публикация #5 |
| */5 min | Проверка pending постов (на случай пропуска) |

**Функции:**

```python
generate_daily_posts()   # RSS → Filter → Generate → Queue
publish_scheduled_post() # Queue → Telegram Channel
send_digest()            # Legacy: создаёт и шлёт 1 дайджест
```

---

## Пайплайн Phase 2

```
┌─────────────────────────────────────────────────────────────────┐
│                        08:00 MSK                                │
│  RSS Feeds → RSSParser.fetch_recent_news()                      │
│      ↓                                                          │
│  Database.filter_unsent_articles()                              │
│      ↓                                                          │
│  PostGenerator.classify_article() [Haiku] → Фильтр релевантных  │
│      ↓                                                          │
│  PostGenerator.generate_post() [Sonnet] → 5 постов              │
│      ↓                                                          │
│  PostQueue.schedule_posts_for_day() → SQLite очередь            │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                   09:00 / 12:00 / 15:00 / 18:00 / 21:00        │
│  PostQueue.get_next_pending()                                   │
│      ↓                                                          │
│  [TODO] ImageGenerator.generate() → GPT Image 1 Mini            │
│      ↓                                                          │
│  TelegramSender.send_to_channel() / send_photo()                │
│      ↓                                                          │
│  PostQueue.mark_published()                                     │
└─────────────────────────────────────────────────────────────────┘
```

---

## Форматы постов

### AI-находка дня
```
🤖 AI-находка дня: [Название]

[Что это — 1 предложение]
[Какую проблему решает]
[Главная фишка]

💰 [Цена]
✅ Попробовать: [ссылка]

🔥 — уже пробовала
💾 — сохраню на будущее
```

### Быстрый совет
```
⚡ Быстрый совет

[Микро-лайфхак 2-3 предложения]

[Эмодзи + результат]
```

### Промт дня
```
🎯 Промт дня: [Тема]

Ситуация: [когда пригодится]

Промт для ChatGPT:
"[готовый промт]"

Копируй и пользуйся! 📋
```

---

## Деплой

### VPS (текущий)

```
Сервер: 141.227.152.143 (OVH)
Путь: /opt/news-assistant-bot
Сервис: news-bot.service (systemd)
```

**Команды:**
```bash
# SSH
ssh root@141.227.152.143

# Статус
systemctl status news-bot

# Логи
journalctl -u news-bot -f

# Рестарт
systemctl restart news-bot
```

### Обновление на VPS

```bash
# Локально (Windows)
scp -r src/*.py root@141.227.152.143:/opt/news-assistant-bot/src/
scp scheduler.py root@141.227.152.143:/opt/news-assistant-bot/

# На сервере
ssh root@141.227.152.143 "systemctl restart news-bot"
```

---

## Зависимости

```
feedparser>=6.0.11
anthropic>=0.76.0
python-telegram-bot>=21.7
python-dotenv>=1.0.1
schedule>=1.2.2
requests>=2.32.3
pydantic>=2.0.0
pydantic-settings>=2.0.0
tenacity>=8.2.0
openai>=1.0.0          # для GPT Image 1 Mini
```

---

## Экономика

| Статья | Стоимость/мес |
|--------|---------------|
| Claude Haiku (классификация) | ~$1-2 |
| Claude Sonnet (генерация) | ~$3-5 |
| GPT Image 1 Mini (150 картинок × $0.013) | ~$2 |
| VPS (OVH) | €3.50 |
| **ИТОГО** | **$9-13** |

---

## TODO (Phase 2)

- [x] post_generator.py — генерация постов
- [x] post_queue.py — очередь в SQLite
- [x] scheduler.py — 5 публикаций/день
- [x] Команды /generate, /preview, /publish_now
- [ ] image_generator.py — GPT Image 1 Mini интеграция
- [ ] sendPhoto вместо sendMessage
- [ ] Деплой Phase 2 на VPS
- [ ] QA-проверка (Haiku)
- [ ] Буфер на 3 дня вперёд
